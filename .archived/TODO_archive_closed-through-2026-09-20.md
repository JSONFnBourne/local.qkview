# TODO archive — bodies of tickets resolved in RT

Moved out of `TODO.md` by `tools/todo_archive.py` (RT#184). Nothing is deleted:
each item is verbatim, under its original section heading. State lives in RT;
this file is the evidence and rollback material those tickets carried.

<!-- swept 2026-09-20 by tools/todo_archive.py (RT#184) -->

## High

- **RT#35** (Session 7) **Decide whether VELOS *controller* archives should show an _aggregated_ chassis-wide tenant inventory synthesized from the partition subpackages.** Session 7 made the tenant table data-driven (renders for any F5OS archive carrying `show tenants`), but the only controller fixture (`syscon.tar`) carries 0 native tenants — a controller qkview doesn't emit `show tenants`; tenants live on the partitions. So the UI change is correct but invisible on syscon.tar. Surfacing a real chassis-wide inventory would require cross-subpackage aggregation in the extractor (read partition/peer-qkview tenant data from the controller bundle). Bigger backend task — confirm scope with user before building.
  - **BUILT 2026-09-19 (Session 11), operator ruling "caveat + partitions table".** A controller archive cannot answer *tenants*, so it now answers what it does carry: `_parse_f5os_partitions()` reads `show partitions` (fixed-width, 8 columns, one row per controller so an HA pair continues the row above it) into `F5OSOverview.partitions`, the reporter emits it, and the page renders a **Chassis Partitions** table plus a caveat line naming the partition archive as where tenants live. The parser **refuses** (returns `[]`) on any header it was not written against rather than slicing a future F5OS layout at stale offsets. 9 tests with synthetic partition names; verified in a real browser against the controller archive — table rendered `none / no controller assigned` and the HA pair as `1 running-active` + `2 running-standby`.
  - **Measured 2026-09-18 (Session 10): there is nothing to aggregate from.** `syscon.tar`'s 182 manifests carry no tenant command at all (vcc-confd has `show partitions`, `show slots`, `show cluster`… but no `show tenants`), and the only nested bundle is the *peer controller*, not a partition. A chassis-wide tenant view needs multiple uploads correlated — a product decision. Options and a recommendation are on the ticket; awaiting the ruling.


## Medium

- **RT#36** (Session 4) Add a *UI-driven* delete for `backend/logs_db/` — a per-row delete action that wipes both the `analyses` row and the matching `logs_<id>.db`. (Session 7 added the startup orphan sweep; the per-row UI action is still open.)
  - **Built 2026-09-18 (Session 10).** There was no list of analyses anywhere in the UI to hang a "per-row" action on, so the row had to be built first: `GET /api/qkview` (newest first, with each row's `logs_<id>.db` size) and `DELETE /api/qkview/{id}` (row + `analysis_files` + index, 404 on unknown) in [backend/main.py](backend/main.py); Next proxies `app/api/qkview/route.ts` and `app/api/qkview/[id]/route.ts`; the `RecentAnalyses` panel on the analyzer page with a two-step confirm, the open analysis marked and not deletable. Verified: 6 unit tests (`test_analysis_delete.py`), live `DELETE` on a scratch store reclaiming a 125 MB index, and the whole stack driven through the Next production server on outcome (`analyze → list → delete 200 → delete 404 → list empty`). `npm run build` clean. **Clicked in a browser 2026-09-19 (Session 11)** via `scripts/browser_smoke.mjs`: 2 rows → trash icon → two-step confirm appeared → Confirm delete → 1 row, with the backend reclaiming the index file. RT#36 is done.

- **RT#37** — ✅ closed 2026-09-19. All five captures audited (v4, described as already reviewed, still carried the domain — the review was the thing to redo). Scrubbed by RT#38, then the originals deleted by the operator; verified gone, with only `qkview/har_scrubbed/` left. Note it removed ~8 MB of that customer's data and not the customer: **RT#338** establishes that three of the F5OS archives are the same customer's chassis. Whether the scrubbed copies may be committed is **RT#339**.
  - **Audit complete 2026-09-18 (Session 10).** Local-only confirmed again (gitignored, never tracked). The customer data is narrower than feared: the streamed `/api/analyze` bodies are empty in every capture (Chrome does not keep streamed NDJSON), so what remains is drill-down URL paths, their small JSON responses and the `X-Filename` values. All five are now scrubbed by RT#38. **Open on one operator decision:** the originals are still on disk and are the last copy of that customer's data here; the `rm` line is on the ticket.

- **RT#38** (Session 1) Build a HAR scrub helper that rewrites customer hostnames / IP ranges / device-name prefixes observed in captured HARs to RFC5737 addresses and synthetic names, so a sanitized capture can live in-tree for CI/benchmarking. Keep the real-pattern list in a local, gitignored note — not in repo docs.
  - **Built 2026-09-18 (Session 10): [scripts/har_scrub.py](scripts/har_scrub.py)**, stdlib only. Hostname rule (allow-listed suffixes; boundaries admit `_` because TMOS names embed FQDNs as `host.com_443_tt`; no `app` TLD because iApp folders are `x.app`), IPv4 rule into RFC 5737 then 198.18/15 with the same table for `vs_10_1_2_3` forms, gitignored literal/regex note (`.har_scrub_patterns.txt`), cookie/auth blanking, `X-Filename` → `archiveN.ext`, and a verification pass that refuses to write if any mapped identifier survives. 20 tests, every guard negative-tested. Ran over all five captures with `--drop-static`: outputs in `qkview/har_scrubbed/` (still gitignored twice over), `--verify-only` CLEAN 5/5. The map of real→synthetic values is `.har_scrub_map.json` (0600, gitignored).

- **RT#39** (Session 1) Investigate the `vCMP.tgz` 77 s and `partition.tar` 64 s receive times — response payloads (1.4–1.5 MB) are not that much larger than `tmos_ve.qkview` (236 KB / 14 s). Find whether the cost is in the streaming producer (extractor / rule engine) or in tar handling for nested layouts.
  - **Answered 2026-09-18 (Session 10) with [scripts/profile_analyze.py](scripts/profile_analyze.py).** Producer, and specifically log volume: partition.tar has 2.38 M log entries to tmos_ve's 196 K, and parse (14 s) + index (22.5 s) + rule scan (13 s) are 83 % of its 59.7 s; nested-tar extraction is 9 s (15 %); serialisation, the client trim and HTTP transport together are under 0.1 s (HTTP-mode gap last-progress→result 0.085 s, 15.11 s end-to-end vs 14.82 s in-process). Peak RSS 5.25 GB. Two leads for later, not done: the rule scan has a ~9 s floor that is 64 % of every small-archive run (per-rule fixed cost, not per-entry), and bulk_insert runs ~106 K rows/s. `QKVIEW_DB_PATH`/`QKVIEW_LOGS_DB_DIR` were added so profiling never writes to the real DB.



## Low

- **RT#41** (Session 2) Replace or drop `"lint": "next lint"` in [webapp/package.json](webapp/package.json) — `next lint` is deprecated in Next.js 16 and errors out with "Invalid project directory". Either switch to `eslint .` or drop the script and rely on `next build`'s TypeScript check.
  - **Verified 2026-09-18 (Session 10)** on outcome (node 22.22.1) after a fresh `npm ci`: `npm run lint` executes `eslint .`, loads `eslint.config.mjs`, and reports 33 real problems in `app/` — those are **RT#334**, not this ticket. Two traps on the way: the copied `node_modules/.bin/eslint` had no exec bit so npm fell through to a global ESLint 6 that cannot read flat config, and as a flattened file it then failed on `require('../package.json')`; both are the degraded-copy problem filed as **RT#336**. The config's own `import/no-anonymous-default-export` warning was fixed in the same pass.

