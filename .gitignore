# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
next-env.d.ts.bak
# Pre-edit backups (the operating standard says back a file up before editing
# it). Suffixed forms too: `rt sync --write` writes TODO.md.bak-rtsync-<ts> on
# every run, and a CLAUDE.md.bak-rt203 reached a commit here before this rule.
*.bak
*.bak2
*.bak-*
*.tsbuildinfo

# Runtime data — real customer QKView archives, DBs, local test inputs
# Never push any of this: archives carry customer IPs, cert CNs, hostnames, pool members.
backend/local_qkview.db
backend/local_qkview.db-journal
backend/local_qkview.db-wal
backend/local_qkview.db-shm
backend/logs_db/
uploads/
*.qkview
*.tgz
*.tar.gz
*.tar
/qkview/

# Local debug captures — may contain cookies, auth headers, customer URLs
*.har
localhost_*.png
screenshot*.png
screenshot*.jpg

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.swp
*.swo

# Secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
